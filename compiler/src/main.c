#include <selena.h>

sln_exit_code_t main(int argc, char* argv[]) {

    sln_utils_cli_color_enable(stdout);

    if (argc < 2) {
        
        return SLN_EXIT_SUCCESS;
    }


    return SLN_EXIT_SUCCESS;
}